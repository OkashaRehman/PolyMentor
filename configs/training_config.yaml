# PolyMentor no longer trains a local checkpoint by default.
# The main model is a Groq-hosted chat model configured in model_config.yaml.
#
# Keep this file as a placeholder for future prompt evaluation or fine-tuning
# experiments, but do not use it as the main project path.

evaluation_notes:
  focus:
    - "Does the chatbot teach clearly?"
    - "Does it identify likely bugs?"
    - "Does it produce runnable multi-language code?"
    - "Does it ask clarifying questions when the task is ambiguous?"
